use crate::layout::{Constraints, Layout};

use super::Dom;

pub fn calculate(dom: &Dom) -> Layout {
    let constraints = Constraints {
        min: None,
        max: None,
    };

    for id in &dom.roots {}

    todo!()
}
